import { Option } from "./Option";

/**
 * Options
 * @targetNSAlias `xsd1`
 * @targetNamespace `urn:loewe.de:RemoteTV:Tablet`
 */
export interface Options {
    /** Option[] */
    Option?: Array<Option>;
}
