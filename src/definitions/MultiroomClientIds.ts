import { MultiroomClientId } from "./MultiroomClientId";

/**
 * MultiroomClientIds
 * @targetNSAlias `xsd1`
 * @targetNamespace `urn:loewe.de:RemoteTV:Tablet`
 */
export interface MultiroomClientIds {
    /** MultiroomClientId[] */
    MultiroomClientId?: Array<MultiroomClientId>;
}
